server:
  port: 8082

spring:
  application:
    name: vega-user-service
  main:
    allow-circular-references: true

  datasource:
    url: jdbc:mysql://localhost:3306/vega_user_db
    username: root
    password: ewnlnqwmkssh52
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  security:
    user:
      name: admin
      password: admin123

jwt:
  secret: vega-super-secret-key-change-in-production
  expiration: 86400000 # 24 hours in milliseconds

logging:
  level:
    com.vega: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
